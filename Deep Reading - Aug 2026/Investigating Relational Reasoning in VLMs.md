---
user_id: "cheng tan"
paper_id: 9303
arxiv_id: "2608.23518v1"
title: "Investigating Relational Reasoning in VLMs"
publish_date: "2026-08-24"
pdf_path: "/Users/mario/Downloads/Projects/PaperFlow/data/exports/Daily Note 2026/arXiv - Aug 2026/2608.23518v1.pdf"
pdf_url: "https://arxiv.org/pdf/2608.23518v1"
generation_provider: "openai-compatible"
generation_model: "deepseek-v4-flash"
response_language: "zh"
report_version: "2026-06-17-v6"
daily_note_path: "/Users/mario/Downloads/Projects/PaperFlow/data/exports/Daily Note 2026/Daily Note - Aug 2026.md"
saved_at: "2026-08-29T01:22:02"
---
# Investigating Relational Reasoning in VLMs

> ★★★★☆ 推荐阅读 · 模型 openai-compatible/deepseek-v4-flash

🏷 关键词：vision-language models · relational reasoning · mechanical interpretability · shortcut learning

## 一句话总结

本文通过合成几何图形数据集、语言线索控制查询和掩码消融实验，结合对 Qwen3-VL-4B 跨层注意力与表征的分析，检验视觉语言模型是否真正理解视觉关系还是依赖语言捷径，发现模型同时使用真实视觉推理与以语言线索为主的捷径策略。

## 摘要

> Vision-Language Models (VLMs) achieve strong performance in visual reasoning tasks, but it remains unclear whether they understand visual relations, or simply employ shortcuts such as language cues or priors. To investigate this, we use the Qwen3-VL-4B (Bai et al., 2025), a modern VLM, to decode how visual information is encoded across depths. For this, we propose a synthetic dataset of simple geometric shapes for controlled analysis, along with queries crafted to precisely test language cues. Furthermore, the dataset is modified to test causal reliance on visual evidence. Our results show that current VLMs combine genuine visual reasoning with shortcut strategies primarily rooted in language cues.

Q1: 这篇论文试图解决什么问题？

1. **核心问题**：VLM 在视觉-语言推理（如关系判断）上的高准确率是否来自对视觉关系的真实理解，还是来自数据中的语言捷径（例如物体名称、空间词等统计关联）或模型内部语言先验？
2. **因果归因难题**：传统评测只报告准确率，无法区分模型是‘看到了关系’还是‘猜出了关系’。要回答这一问题，需要可控的刺激、精确的语言提示变体和因果干预（如遮挡视觉证据），而现有基准大多缺少这种设计。
3. **内部机制黑箱**：即使行为上表现良好，VLM 如何在注意力与表征层面编码‘左/右/上/下’等关系，以及这种编码是否随层深度变化，尚不清楚。论文试图在架构层面（跨层）理解视觉关系信息的表征方式。
4. **科学方法缺口**：已有工作多关注分类或图文匹配，对‘关系级’视觉推理的深层机制研究不足；合成几何图形能隔离视觉关系的最小成分（primitive、属性、关系），为机制分析提供干净实验场。

Q2: 有哪些相关研究？

1. **VLM 视觉推理与图表理解**：相关研究包括 Lu et al. (2023) 和 Zhang et al. (2024) 对图表、示意图等视觉语言理解工作的探索；Hou et al. (2024) 的工作也被引用，但检索证据未给出具体方向（合理推断其涉及视觉推理或图表理解）。本文在受控合成图形上做机制分析，比自然图表更便于隔离变量。
2. **机械可解释性（Mechanistic Interpretability）**：已有工作证明 transformer 注意力模式能编码结构依赖，可用于分析语言模型中的句法与组合推理（如 Clark et al. 2019 的 BERT 注意力分析）。本文沿用这一思路，将其扩展到 VLM 的关系推理场景。
3. **VLM 中的注意力分析**：部分研究已用基于注意力的方法分析 VLM 的跨模态交互，但往往只关注高层语义，或仅用自然图像。本文用合成图形，使关系变量可控，能更直接地检验‘注意力是否指向关系所涉及的两个对象’。
4. **语言捷径与先验**：已有文献表明 VLM 在 VQA、视觉推理中会利用问题中的语言线索或训练集统计偏置。本文的贡献在于将语言线索测试嵌入合成数据集，并通过遮挡视觉元素来量化因果依赖。
5. **合成数据与受控评测**：视觉推理领域常用合成数据（如 CLEVR 类）来消除背景混杂。本文强调更简单的几何形状，以便在深度层面分析表征，并特别构造与语言线索正交的测试对，这一点在现有合成基准中较少见（推测）。

Q3: 论文如何解决这个问题？

1. **模型与总思路**：选用 Qwen3-VL-4B 作为现代 VLM 代表，结合( a )行为评测与( b )内部机制分析两个层面，判断模型是否真正使用视觉关系信息。
2. **构建合成数据集**：由简单几何形状组成，每个样本包含明确的基本视觉符号（primitive）、属性（颜色、大小、形状）和它们之间的关系（如空间方位、大小比较等）。合成数据的可控性使研究者能精确操纵视觉证据和语言提示。
3. **语言线索探测设计**：构造特殊查询，使得仅凭语言线索（如物体名称、位置词）可能与视觉证据一致或不一致，从而分离‘语言捷径’与‘真实视觉理解’。若模型在语言提示足够但视觉信息缺失时仍能答对，就说明它依赖语言；若移除视觉证据后性能显著下降，则说明依赖视觉。
4. **因果依赖测试（掩码变体）**：修改数据集，遮挡或移除某些显式视觉元素（如关系相关的对象），形成掩码版本，再重复实验。通过对比原始与掩码数据上的性能，评估模型对视觉证据的因果依赖程度。
5. **跨层分析**：解码视觉信息如何在不同深度被编码。具体可能包括：提取各层（或块）的注意力权重、中间表征，观察关系相关的模式是否随层数变化；也可能使用 probing 或注意力可视化，检验模型是否在隐层中显式表征‘关系’。论文引言提到‘在架构层面理解’与‘关系任务未出现一致模式’，说明作者从 attention 模式中寻找结构性证据（合理推断）。

Q4: 论文做了哪些实验？

1. **行为实验（原始合成数据集）**：在包含多种关系类型（如空间方位、比较关系）的合成数据上测试 Qwen3-VL-4B 的关系推理准确率。具体数值未在检索证据中出现。
2. **语言线索控制实验**：设计不同措辞的查询，使语言提示强度与视觉证据不一致，观察准确率变化，以评估语言捷径的使用程度（合理推断）。
3. **掩码变体消融实验**：按照 3.1.3 节描述的掩码数据集设置，将视觉元素遮挡后重新执行相同实验，结果汇总在表 4.3。该实验用于区分‘高准确率是真正视觉推理还是语言猜测’。检索证据只给出了实验目的和表编号，未提供具体结果数字。
4. **跨层机制分析**：分析 transformer 各层（或多层注意力头）对视觉关系的编码，观察是否出现稳定的、与关系结构对应的注意力模式。论文提到‘关系任务没有展现出一致的模式’，说明可能比较了不同层、不同关系类型下的注意力模式（推测）。
5. **消融/对照比较**：除掩码外，可能还比较了不同提示模板、不同关系类型、不同层输出等，但细节在检索材料中不完整。所有数值结果在现有证据中不完整，需阅读原文表 4.3 及对应章节确认。

Q5: 发现了什么实验现象？

1. **混合策略而非纯捷径**：摘要明确总结‘当前 VLM 将真实视觉推理与主要基于语言线索的捷径策略相结合’，说明行为表现既有来自视觉处理的贡献，也有语言先验的贡献。
2. **关系任务无一致注意力模式**：引言片段指出‘……在架构层面被理解，但关系任务没有展示出一致的模式’。这意味着与语言句法/组合任务相比，视觉关系在注意力层面缺乏清晰、系统的结构模式，可能是关系理解不够‘显式’。
3. **对显式视觉元素的强依赖**：第三个发现是‘VLM 对显式视觉元素的存在表现出强烈依赖’。这可能说明只要相关物体可见，模型就能利用视觉信息；一旦遮挡，性能明显下降（推测），也暗示其视觉推理偏‘局部’而非‘全局关系’。
4. **消融实验中的相对效应**：掩码变体实验用于检验高准确率是否真实。从证据看，作者认为掩码后性能变化揭示了捷径的贡献，但具体幅度未记录在检索片段中。
5. **指标间张力**：高行为准确率与注意力模式缺乏一致性形成对照——模型能答对问题，却没有可解释的稳定关系表征，这提示‘行为正确’与‘机制理解’之间可能存在显著差距。

Q6: 有什么可以进一步探索的点？

1. **扩展模型范围**：本文只研究 Qwen3-VL-4B，未来可在更大或不同架构的 VLM（如 Qwen2-VL、LLaVA、GPT-4V 类）上检验结论是否普遍。
2. **更多关系类型与复杂度**：几何形状关系较简单，可扩展到遮挡、多对象、尺寸、计数、逻辑组合等更复杂关系，以及真实世界图像，以检验机制发现的迁移性。
3. **更强的干预手段**：除掩码外，可使用因果特征归因、激活编辑、注意力头剪枝、通过视觉 token 替换等干预，进一步验证因果关系。
4. **训练层面的改进**：理解模型对显式视觉元素的依赖后，可设计训练策略（如视觉-语言对比、反事实训练）促使模型形成更稳健的关系表征，减少语言捷径依赖。
5. **更深层表征分析**：结合 probing、线性表示、关系分类器，检验关系是否以线性方向等形式编码在隐空间中，从而补充注意力模式结果。
6. **与智能体结合**：在视觉-语言智能体的规划与决策中，关系推理常是不可或缺的环节；可将本文的评测协议用于评估智能体感知模块的可靠性，或指导智能体在视觉证据不完整时的应对策略。
7. **跨模态与多语种语言线索**：本文聚焦语言提示；未来可研究语言先验源于训练语料中的哪些分布特征，并尝试在多语种、弱监督设置下消除先验偏差。

Q7: 总结一下论文的主要内容

论文题为《Investigating Relational Reasoning in VLMs》，旨在回答一个基本问题：视觉语言模型在关系推理任务中的高表现，究竟来自对视觉关系的真实理解，还是来自语言捷径（如问题措辞中的提示、物体名称的统计关联）或内部语言先验？作者认为，仅看准确率无法回答这个问题，必须在受控条件下分离视觉证据和语言提示，并通过因果干预确认模型是否‘真的在看’。

**技术主线**：作者选择现代 VLM Qwen3-VL-4B，提出一个合成数据集——由简单几何形状构成，每个样本包含基本视觉符号（primitive）、属性（颜色、大小、形状）以及它们之间的关系（如空间方位、大小比较）。合成设计使得视觉关系可以精确控制，并避免真实图像的背景干扰。为了测试语言捷径，他们设计了特殊查询，使语言线索的可用性与视觉证据形成对照；为了测试因果依赖，他们修改数据集生成掩码变体，遮挡显式视觉元素，重复实验，以观察模型在没有视觉证据时是否仍能作答。

**实验与机制分析主线**：论文进行了行为实验（原始与掩码数据集）和跨层机制分析。行为实验关注不同条件下的准确率差异，特别是掩码消融（结果见表 4.3），用于判断高准确率是否基于真实视觉推理。机制分析关注 attention 模式与中间表征如何随层深度变化，检验关系信息是否以结构性方式编码。结果发现：(i) 模型在架构层面对某些视觉符号或属性有编码，但关系任务没有呈现一致的、稳定的注意力模式；(ii) 模型显著依赖显式视觉元素的存在，移除元素会明显改变行为； (iii) 总体策略是真实视觉推理与语言痕迹的混合，语言线索起着重要作用。

**结论与意义**：作者认为这些发现确认了先前工作关于 VLM 使用捷径的结论，并将理解推进到架构层面——即关系推理缺乏可解释的一致内部模式，同时显式视觉依赖较强。这为后续改进 VLM 的视觉关系理解提供了切入点：需要更强的关系表征约束，而非仅仅依赖显式对象特征。论文也展示了合成数据+干预实验+机械可解释性相结合的方法论价值，对评测和解释 VLM 的‘视觉理解’具有参考意义。

**局限（基于现有材料）**：只测试了一个模型；合成图形相对简单；掩码变体主要遮挡显式元素，未覆盖更抽象的关系干预；检索证据未提供表 4.3 等具体数值，需读原文验证结论强度。

## 推荐指数

★★★★☆（4/5）
- 推荐理由：对智能体方向：VLM 作为智能体感知模块时，关系推理质量直接影响规划与交互；本文的评测协议可用于构建更可靠的视觉感知测试，或诊断智能体是否在‘假看真猜’。

## 基本信息

- 作者：Adhithya Laxman Ravi Shankar Geetha, Aulia Kharis Rakhmasari, Haleema Ramzan, Xander Yap
- 机构：未提供
- 来源：arxiv
- 主题/分类：cs.CV
- 日期：2026-08-24
- 推荐级别：**推荐阅读**
- 解析来源：摘要 + 元数据
- 生成模型：openai-compatible / deepseek-v4-flash
- arXiv ID：`2608.23518v1`
- 解析说明：本报告当前基于摘要和元数据自动生成，方法与实验细节建议回到原文核对。 已结合全文切块语义检索证据生成。 当前精读正文已将用户兴趣 embedding 检索链路作为主要证据排序信号之一。 本次生成参考了 PDF 检索证据（abstract、introduction、related work、ablation 章节片段及 references），并在关键推断处标注了‘合理推断’或‘推测’；由于检索片段不完整，部分实验中具体数值和消融幅度需查阅原文确认。
