---
user_id: "cheng tan"
paper_id: 9004
arxiv_id: "2608.19726"
title: "Projector Is All You Train"
publish_date: "2026-08-21"
pdf_path: "/Users/mario/Downloads/Projects/PaperFlow/data/exports/Daily Note 2026/arXiv - Aug 2026/2608.19726.pdf"
pdf_url: "https://arxiv.org/pdf/2608.19726"
abs_url: "https://arxiv.org/abs/2608.19726"
generation_provider: "openai-compatible"
generation_model: "deepseek-v4-flash"
response_language: "zh"
report_version: "2026-06-17-v6"
daily_note_path: "/Users/mario/Downloads/Projects/PaperFlow/data/exports/Daily Note 2026/Daily Note - Aug 2026.md"
saved_at: "2026-08-21T23:53:05"
---
# Projector Is All You Train

> ★★★★☆ 推荐阅读 · 模型 openai-compatible/deepseek-v4-flash

🏷 关键词：multimodal large language model · projector-only training · D point cloud understanding · parameter-efficient adaptation

## 一句话总结

该论文通过3D多模态大语言模型（3D MLLM）的系统实验证明，在引入新模态时仅训练投影器（projector）而不微调语言模型主干，即可取得与联合训练相当甚至更优的多模态性能，同时避免语言模型原有能力的灾难性遗忘，并带来约两倍的训练吞吐量提升。

## 摘要

> The typical training process of a multimodal large language model (MLLM) involves adapting both the language model backbone and the projector between the backbone and a modality-specific encoder. We ask whether fine-tuning the backbone of an MLLM is necessary to adapt it to a new modality. Through experiments on 3D MLLMs, we find that training only the projector is sufficient to achieve strong multimodal performance relative to existing baseline models and our jointly trained MLLMs with the same encoder and backbone. We also show that joint training leads to undesirable drift in existing capabilities of the language model, which projector-only training avoids by definition. Furthermore, projector-only training has approximately twice the training sample throughput of joint training. We validate our findings across different language model backbones via 3D classification and captioning benchmarks as well as standard benchmarks evaluating language, vision, and spatial reasoning capabilities.

Q1: 这篇论文试图解决什么问题？

1. **核心问题**：MLLM训练中，为适配新模态（如3D点云），是否必须同时微调语言模型主干和投影器？现有范式默认两者都需要端到端联合训练，但缺乏对主干微调必要性的系统验证。
2. **背景矛盾**：联合训练虽然能带来模态对齐，但可能破坏语言模型在预训练阶段习得的通用能力（catastrophic forgetting / capability drift），且训练成本高（需要更新大量参数）。
3. **研究空白**：此前工作（如PointLLM等）几乎都采用联合微调范式，尚未有研究在3D模态上系统比较仅训练投影器与联合训练的性能差异，也未从能力保持和训练效率角度量化两种策略的权衡。
4. **本文问题定义**：将新模态接入预训练语言模型时，主干是否需要参与训练？若不需要，则意味着模态适配可以解耦为独立训练的投影器模块，这将对MLLM的训练范式、模块复用和持续扩展产生深远影响。
5. **隐含假设**：投影器有能力将任意模态特征映射到语言模型的语义空间，且语言模型本身具备足够的先验知识来解读该映射，无需针对新模态调整内部表示。本文通过实验检验这一假设。

Q2: 有哪些相关研究？

1. **多模态大语言模型（MLLM）**：主流方法通过投影器将视觉/音频/3D等模态编码器的输出映射到语言模型嵌入空间，典型代表包括LLaVA、MiniGPT-4等（图片、音频、3D均有扩展）。统一范式是“编码器+投影器+语言模型”三层结构。
2. **3D MLLM**：将3D点云编码器与LLM结合的工作，如PointLLM、3D-LLM等，通常采用两阶段训练：先投影器对齐，再联合微调。本文的baseline（PointLLM等）均在该领域有代表性。
3. **参数高效微调（PEFT）**：LoRA、Adapter、Prefix-tuning等方法尝试减少可训练参数。本文的“仅训练投影器”属于更极端的冻结主干方案，但区别在于完全不更新语言模型任何参数，只更新投影器。
4. **能力漂移与灾难性遗忘**：在指令微调或多模态微调中，语言模型可能出现通用能力退化。相关研究关注如何保持原有能力，例如通过混合训练、回放或正则化。本文从“冻结主干”角度彻底避免该问题。
5. **提示工程（Prompt Engineering）**：作者在结论中提出，优化投影器输出可类比于为模型找到更好的“软提示”，解释仅训练投影器为何有效——投影器在隐空间构造输入特征，类似于对语言模型进行隐式提示。
6. **模态对齐方法**：此前工作强调联合训练有助于模态间深度对齐，但本文用实验表明仅投影器也能达到足够对齐，挑战了“深度对齐必须有主干更新”的隐含假设。

Q3: 论文如何解决这个问题？

1. **总体思路**：不更新语言模型主干（冻结），仅训练投影器。将3D点云编码器的输出映射到语言模型嵌入空间，并通过任务损失（分类/描述生成）优化投影器参数。
2. **训练策略**：采用监督学习，在3D分类和3D描述生成任务上训练投影器，语言模型和点云编码器保持冻结。为了公平对比，还训练了相同架构但联合微调语言模型和投影器的模型（joint training）。
3. **实验设计**：
 - 变量控制：使用相同的3D点云编码器、相同的语言模型主干、相同的数据集，仅改变“是否训练主干”这一变量。
 - 主干多样性：在多个语言模型主手上重复实验，验证结论的普适性。
 - 评测维度：不仅评测3D任务（分类、描述），还评测标准语言、视觉、空间推理基准，以检测能力漂移。
4. **效率度量**：比较仅训练投影器与联合训练的样本吞吐量（training sample throughput），量化计算开销差异。
5. **解释性分析**：从提示工程角度提出假说——投影器输出的优化相当于在隐空间构造更有效的“软提示”，使得冻结语言模型也能解读新模态。
6. **分类与描述生成任务的具体实现**（合理推断）：分类任务可能通过对语言模型输出的类别词概率进行判别；描述生成使用标准语言建模损失。
7. **对比基线**：包括现有的PointLLM等3D MLLM，以及本文复现的联合训练模型。

Q4: 论文做了哪些实验？

1. **数据集**：3D分类和3D描述生成使用标准3D数据集（如ModelNet40、ShapeNet等，具体数据集名在检索片段未出现，需回原文确认）。
2. **语言模型主干**：测试了多种不同规模/类型的语言模型主干（具体名称未在证据中给出，合理推断包括LLaMA系列或其他开源模型），以验证结论的跨主干一致性。
3. **3D任务评测**：
 - 分类准确率：与现有方法（如PointLLM）和联合训练模型比较。
 - 描述生成质量：使用相关指标（如CIDEr、BLEU等，具体指标未提及）。
4. **标准能力评测**：语言理解、视觉理解、空间推理等标准基准，比较仅训练投影器与联合训练后的得分，检验能力漂移。
5. **效率实验**：测量训练吞吐量（样本/秒），对比两种训练策略。
6. **阈值分析**：在分类任务中，观察到不同时间阈值（time thresholds）下的准确率表现，推测可能涉及训练步数或推理时采样阈值。
7. **对比现有最优方法**：在检索片段中可见，所有仅训练投影器的模型得分均达到或超过PointLLM，且PointLLM的分数位于……（图像证据，具体数值未给出）。
8. **多主干实验**：跨不同语言模型主干重复上述流程，确保结论不是特定主干下的偶然现象。

Q5: 发现了什么实验现象？

1. **仅训练投影器与联合训练竞争力相当**：在大多数测试阈值上，仅训练投影器得分更高，部分阈值下差距可忽略不计。特别地，对于ModelNet40（推断为M40.），仅训练投影器一致优于联合训练。
2. **超越现有方法**：所有仅训练投影器的3D MLLM在性能上达到或超过PointLLM，而PointLLM的得分全部低于仅训练投影器的模型（基于图表证据）。
3. **避免能力漂移**：联合训练会导致语言模型原有能力（语言、视觉、空间推理）出现不良漂移；仅训练投影器由于不更新主干，从定义上避免了这种漂移，且在标准基准上保持原有能力。
4. **训练效率翻倍**：仅训练投影器的样本吞吐量约为联合训练的两倍，源于反向传播不需要经过大型语言模型主干。
5. **反直觉现象**：尽管联合训练允许语言模型调整内部表示以适配新模态，但最终性能并未因此更强，反而可能因过度适配而损失通用能力。这挑战了“模态适配必须深度联合优化”的直觉。
6. **提示工程类比**：作者推测仅训练投影器有效的机制类似于提示工程——投影器输出的高维向量充当了语言模型的“软提示”，冻结语言模型仍能有效解读。
7. **指标间张力**：联合训练在3D任务上可能略优（某些阈值下），但以牺牲通用能力为代价；仅训练投影器在通用能力保持和效率上占优，且3D性能大多数情况下更好或相当，体现了最佳权衡。
8. **负结果/边界**：某些时间阈值下两者差距“可忽略不计”，说明联合训练的优势并不稳定，进一步支持投影器仅训练的有效性。

Q6: 有什么可以进一步探索的点？

1. **扩展到更多模态**：本文限于3D点云编码器，值得验证“仅训练投影器”对图像、音频、视频等其他模态是否同样成立。
2. **大规模主干验证**：测试更大规模语言模型（如10B级甚至更大），观察是否因容量增大而改变结论。
3. **更复杂任务**：当前覆盖3D分类和描述，可探索3D问答、3D视觉推理、具身任务等更复杂场景。
4. **机制研究**：深入理解投影器优化为何有效，例如分析投影器输出与语言模型内部表示的几何关系，验证“软提示”假说。
5. **多模态多投影器共享**：作者提出借助冻结语言模型，可训练多个投影器对应不同模态编码器，未来可研究多模态投影器共享与调度机制。
6. **投影器架构设计**：探索更强大的投影器（如交叉注意力、Q-Former等）是否能进一步缩小或超越联合训练的性能差距。
7. **自动化架构搜索**：基于“仅训练投影器”的廉价训练，可加速对投影器架构和超参的搜索。
8. **混合训练策略**：考虑在后期冻结主干进行“先联合后冻结”或“分阶段训练”，结合两者优势。
9. **理论分析**：从优化和表示学习角度解释为何线性/非线性投影即可映射新模态到语言空间，形成理论保证。
10. **与提示工程结合**：将投影器学习与离散/连续提示优化结合，探索新模态适配与可学习提示的协同。

Q7: 总结一下论文的主要内容

该论文重新审视了多模态大语言模型（MLLM）的标准训练范式，提出一个根本性问题：为将语言模型适配到新模态，微调语言模型主干是否必要？作者在3D MLLM场景中，通过严格的对照实验回答该问题。实验设计保持编码器、主干、数据一致，仅改变训练策略（仅训练投影器 vs 联合微调投影器和主干），并在多个语言模型主干上重复验证。主要发现包括：（1）仅训练投影器即可达到与联合训练相当甚至更优的3D分类和描述性能，且大部分阈值下优于联合训练；（2）所有仅训练投影器的模型在性能上超越现有方法PointLLM；（3）联合训练导致语言模型原有能力（语言、视觉、空间推理）出现不良漂移，而仅训练投影器从定义上规避了该问题；（4）仅训练投影器的训练样本吞吐量约为联合训练的两倍，显著提升训练效率。作者进一步提出解释性假说，将投影器优化类比于提示工程——投影器输出相当于为冻结语言模型构造的“软提示”，从而无需调整主干即可实现模态适配。该工作表明，模态适配可以解耦为独立训练的投影器模块，同一冻结语言模型可作为多种模态编码器的共享主干，各投影器可独立并行训练。作者也指出工作局限于3D点云编码器，未来可扩展至更多模态及更复杂任务。论文的核心贡献是挑战了MLLM中“联合微调主干”的必要性，提供了一种更高效、更保护原有能力、且性能不妥协的训练范式。

## 推荐指数

★★★★☆（4/5）
- 推荐理由：本论文探讨MLLM训练中主干微调必要性的基本问题，对理解多模态表示学习机制有参考价值。

## 基本信息

- 作者：Nyx Iskandar, Saathvik Selvan, Slater Victoroff
- 机构：未提供
- 来源：arxiv
- 主题/分类：cs.CL, cs.CV, cs.LG
- 日期：2026-08-21
- 推荐级别：**推荐阅读**
- 解析来源：摘要 + 元数据
- 生成模型：openai-compatible / deepseek-v4-flash
- arXiv ID：`2608.19726`
- 解析说明：本报告当前基于摘要和元数据自动生成，方法与实验细节建议回到原文核对。 已结合全文切块语义检索证据生成。 当前精读正文已将用户兴趣 embedding 检索链路作为主要证据排序信号之一。 本次生成参考了PDF语义检索证据（摘要、引言、结论、实验结果片段），但证据覆盖有限，部分实验细节系合理推断，建议阅读原文核对数值和设置。
